# Inicializar git
git init
git add .
git commit -m "feat: ReservaFácil inicial"

# Subir a GitHub (crea el repo en github.com primero)
git remote add origin https://github.com/TU_USUARIO/reservafacil.git
git branch -M main
git push -u origin main